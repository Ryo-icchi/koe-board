# koe-board（こえ文字盤）

- 構音障害で発話が難しい方向けの文字盤＋読み上げ＋定型文 PWA。バニラ JS・ビルドなし
- ユーザーは高齢者: 大きいタップ領域・シンプル操作を最優先。機能追加より「迷わず使える」こと
- 変更後は `node test-e2e.mjs` を実行し、ブラウザでも実操作して確認する
- Service Worker は HTML/app.js ネットワーク優先のハイブリッド（更新が確実に届く設計・キャッシュ全振りに戻さない）
- 実機（利用者本人の端末）依存の挙動は「動作OK」と断定せず実機確認待ちで報告する
